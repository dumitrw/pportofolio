---
import "@styles/global.css"
import BaseHead from "@components/BaseHead.astro"
import Header from "@components/Header.astro"
import Footer from "@components/Footer.astro"
import Drawer from "@components/Drawer.astro"
const { title, description } = Astro.props
import { SITE } from "@consts"
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={`${title} | ${SITE.TITLE}`} description={description} />
  </head>
  <body>
    <!-- Verificăm dacă animația a avut loc deja -->
    {sessionStorage.getItem("overlayCompleted") !== "true" ? (
      <div id="overlay">
        <img id="logo" src="/logo2.png" alt="Logo" />
      </div>
    ) : null}

    <Header />
    <Drawer />
    <main>
      <slot />
    </main>
    <Footer />
    
    <!-- Script pentru a elimina overlay-ul după animație -->
    <script>
      document.addEventListener("DOMContentLoaded", () => {
        const overlay = document.getElementById("overlay");
        if (overlay) {
          overlay.addEventListener("animationend", () => {
            overlay.remove(); // Elimină complet overlay-ul din DOM
            sessionStorage.setItem("overlayCompleted", "true"); // Stochează că animația a avut loc
          });
        }
      });
    </script>
  </body>
</html>
